services:
  dev:
    build: .
    working_dir: /workspace
    volumes:
      - .:/workspace
    tty: true
    ports:
      - "127.0.0.1:10043:10043"
    command: ["python3", "/workspace/scripts/cc_server.py"]

